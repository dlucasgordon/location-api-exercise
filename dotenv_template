# This is a template for a `.env` file, which can be used with the `dotenv` npm module to load environment variables
# into the program's `process.env` variable. To use it, copy to `.env`, open the new file, and fill in the values.

SERVER_PORT=5000
