// Given a array of locations in string format, which could be names, addresses, etc,
// return an array of tuples of the original location string, a latitude number, and a longitude number
const geocode = (locations: Array<string>): Array<[string, number, number]> => {
  console.log(locations)
  const loc_coords: Array<[string, number, number]> = []
  return loc_coords
}

export default geocode
